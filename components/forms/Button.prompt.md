General-purpose action button. Not defined in the source deck (a slide template) — an intentional addition for prototyping software UI with this brand.

```jsx
<Button variant="primary">Get Started</Button>
```

Variants: primary (solid accent-violet), secondary (white, divider border), ghost (text-only).
