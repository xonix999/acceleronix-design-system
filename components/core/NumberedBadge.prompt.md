Solid accent-rotation circle with a bold white number, used to order principle cards (01–04).

```jsx
<NumberedBadge number={1} accent="var(--accent-violet)" />
```

Cycle accents violet → indigo → magenta → cyan across a set, one per item.
