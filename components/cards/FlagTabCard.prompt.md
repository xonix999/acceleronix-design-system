Timeline / numbered-principle card: white body with a colored "flag tab" header band, built from layered shapes (not a single asymmetric-radius shape).

```jsx
<FlagTabCard label="2019 — Founded" body="Card supporting copy." accent="var(--accent-violet)" />
```

Cycle `accent` through violet → indigo → magenta → cyan across a set of cards.
